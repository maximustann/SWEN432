consistency LOCAL_QUORUM;

INSERT INTO S.vehicles (
   email_address,
   vehicle_id,
   colour,
   type

)

values (
   'fred@fred.com',
   'xyz123',
   'red',
   'sedn'
);

UPDATE S.vehicle_count
SET
number_of_vehicle = number_of_vehicle + 1
where email_address='fred@fred.com';
